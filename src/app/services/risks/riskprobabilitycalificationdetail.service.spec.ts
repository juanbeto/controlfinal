import { TestBed } from '@angular/core/testing';

import { RiskprobabilitycalificationdetailService } from './riskprobabilitycalificationdetail.service';

describe('RiskprobabilitycalificationdetailService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: RiskprobabilitycalificationdetailService = TestBed.get(RiskprobabilitycalificationdetailService);
    expect(service).toBeTruthy();
  });
});
